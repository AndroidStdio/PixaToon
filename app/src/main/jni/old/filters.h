#include <opencv2/core/core.hpp>
#include <opencv2/imgproc/imgproc.hpp>
#include <opencv2/photo/photo.hpp>


using namespace std;
using namespace cv;

void gradient(Mat& , Mat& );

void quantize(Mat& , Mat& );

void cartoonFilter(Mat& );
void cartoon2Filter(Mat& );
